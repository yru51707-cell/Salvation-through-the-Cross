<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>门前吃饭 · 馋猫望着我</title>
<style>
  :root{
    --sky-top:#e6d5f0;
    --sky-bottom:#fbe0ea;
    --door-frame:#c9a3c9;
    --door-panel:#e8bfd6;
    --ground:#f3c9dc;
    --ground-dark:#e6a8c4;
    --ink:#6b4472;
    --bowl:#ffffff;
    --rice:#fff6ee;
    --cat-body:#b98fc2;
    --cat-belly:#f3d9ea;
    --accent:#a85fa0;
  }
  *{box-sizing:border-box;}
  html,body{
    margin:0;
    height:100%;
    overflow:hidden;
    font-family:"Songti SC","STSong","SimSun",serif;
    background:var(--sky-bottom);
  }
  .scene{
    position:relative;
    width:100vw;
    height:100vh;
    background:linear-gradient(to bottom, var(--sky-top) 0%, var(--sky-bottom) 55%);
    overflow:hidden;
  }

  .orb{
    position:absolute;
    border-radius:50%;
    background:radial-gradient(circle, rgba(255,255,255,0.9) 0%, rgba(255,255,255,0) 70%);
    animation:float 6s ease-in-out infinite;
  }
  .o1{ width:16px; height:16px; top:14%; left:20%; animation-delay:0s; }
  .o2{ width:10px; height:10px; top:22%; left:70%; animation-delay:1.5s; }
  .o3{ width:12px; height:12px; top:10%; left:50%; animation-delay:3s; }
  @keyframes float{
    0%,100%{ transform:translateY(0); opacity:0.6; }
    50%{ transform:translateY(-14px); opacity:1; }
  }

  .poem{
    position:absolute;
    top:6%;
    left:6%;
    color:var(--ink);
    opacity:0.75;
    font-size:14px;
    line-height:2;
    letter-spacing:2px;
    writing-mode:vertical-rl;
    z-index:6;
  }

  .door-frame{
    position:absolute;
    bottom:0;
    left:50%;
    transform:translateX(-50%);
    width:64%;
    max-width:420px;
    height:58%;
    background:var(--door-frame);
    border-radius:40px 40px 0 0;
    z-index:1;
  }
  .door-panel{
    position:absolute;
    bottom:0;
    left:6%;
    width:88%;
    height:94%;
    background:linear-gradient(to bottom, var(--door-panel) 0%, #f3d5e6 100%);
    border-radius:32px 32px 0 0;
  }

  .ground{
    position:absolute;
    bottom:0;
    left:0;
    width:100%;
    height:20%;
    background:linear-gradient(to bottom, var(--ground) 0%, var(--ground-dark) 100%);
    z-index:2;
  }
  .step{
    position:absolute;
    bottom:16%;
    left:0;
    width:100%;
    height:6%;
    background:var(--ground-dark);
    opacity:0.6;
    z-index:2;
  }

  .person{
    position:absolute;
    bottom:15%;
    left:38%;
    width:70px;
    height:64px;
    z-index:4;
  }
  .person .head{
    position:absolute;
    top:0;
    left:23px;
    width:24px;
    height:24px;
    border-radius:50%;
    background:var(--ink);
  }
  .person .hair{
    position:absolute;
    top:-3px;
    left:20px;
    width:30px;
    height:16px;
    background:var(--accent);
    border-radius:50% 50% 0 0;
  }
  .person .body{
    position:absolute;
    top:22px;
    left:10px;
    width:50px;
    height:38px;
    background:var(--accent);
    border-radius:20px 20px 6px 6px;
    animation:sitbreeze 3
